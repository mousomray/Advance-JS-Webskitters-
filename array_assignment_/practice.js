// Assume the age is 20
let age = 20;

// Using the ternary operator to determine if the person is an adult or a minor
let result = (age >= 18) ? 'Adult' : 'Minor';

// Outputting the result
console.log(result); // This will print 'Adult' since age is greater than or equal to 18
