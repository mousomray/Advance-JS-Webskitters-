/* 
i: Case insensitive matching.
g: Global matching (find all matches rather than stopping after the first match).
m: Multiline matching (treats beginning (^) and end ($) characters as working across multiple lines).
s: Allows the dot (.) to match newline characters as well.
u: Enables full Unicode support.
y: Sticky matching (matches only from the index indicated by the lastIndex property of the regex).
y: Sticky matching (matches only from the index indicated by the lastIndex property of the regex).
*/